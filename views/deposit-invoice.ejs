<h2 class="mb-4">Invoice Deposit</h2>

<div class="row">
  <div class="col-md-6 text-center mb-4">
    <div class="card p-4 shadow-sm">
      <h5 class="mb-3">Scan QR / Transfer ke:</h5>
      <img src="<%= metode.gambar %>" class="img-fluid rounded" style="max-height: 300px" alt="QR Code">
      <p class="text-muted mt-3"><strong><%= metode.nama %></strong></p>
    </div>
  </div>

  <div class="col-md-6">
    <div class="card p-4 shadow-sm">
      <h5 class="mb-3">Detail Deposit</h5>

      <ul class="list-group mb-3">
        <li class="list-group-item d-flex justify-content-between">
          <span>Jumlah Awal</span>
          <strong>Rp <%= (total_transfer - metode.kode_unik || 0).toLocaleString('id-ID') %></strong>
        </li>
        <li class="list-group-item d-flex justify-content-between">
          <span>Kode Unik</span>
          <strong class="text-primary"><%= total_transfer %></strong>
        </li>
        <li class="list-group-item d-flex justify-content-between bg-light">
          <span>Total yang harus ditransfer</span>
          <strong class="text-success fs-5">Rp <%= total_transfer.toLocaleString('id-ID') %></strong>
        </li>
      </ul>

      <div class="alert alert-warning">
        <strong>Catatan:</strong>
        <ul class="mb-0 ps-3">
          <li>Transfer sesuai nominal hingga 3 digit terakhir (kode unik).</li>
          <li>Saldo akan otomatis masuk jika pembayaran berhasil terdeteksi.</li>
          <li>Waktu proses max 2 menit.</li>
        </ul>
      </div>

      <div class="text-center mt-4">
        <a href="/dashboard" class="btn btn-outline-secondary">
          <i class="fas fa-arrow-left"></i> Kembali ke Dashboard
        </a>
      </div>
    </div>
  </div>
</div>
